import { Container } from '@mui/material';
import React from 'react';

const About = () => {
    return (
        <Container maxWidth="xl">
            <h1>This is about</h1>
        </Container>
    );
};

export default About;